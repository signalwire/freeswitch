{
    "files.eol": "\n", // Force LF (Linux-style) line endings
    "files.trimTrailingWhitespace": false, // Prevent automatic removal of trailing whitespaces
    "files.insertFinalNewline": false, // Prevent automatic insertion of a newline at the end of the file
    "editor.formatOnSave": false // Disable automatic formatting on save for this project
}