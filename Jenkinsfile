pipeline {
    agent { label 'kubernetes-default' }
    stages {
        stage('Build') {
            steps {
                sh 'echo "Empty build to trigger telnyx_b2bua_builder"'
            }
        }
    }
}